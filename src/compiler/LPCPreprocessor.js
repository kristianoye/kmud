const
    PipelineComponent = require('./PipelineComponent');

class LPCPreprocessor extends PipelineComponent {
    constructor(config) {
        super(config);
    }
}

module.exports = LPCPreprocessor;
